@page
@model FineUI.Core.Examples.RazorPages.Pages.Config.LoadingModel
@{
    ViewBag.Title = "Config/Loading";
}

@using System.Text;


@section head {

    <style type="text/css">
        ul.loading {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

            ul.loading li {
                display: inline-block;
                margin: 0 10px 10px 0;
                text-align: center;
                border-style: solid;
                border-width: 1px;
                width: 150px;
                padding: 10px;
            }

                ul.loading li .title {
                    margin-top: 15px;
                    font-size: 1.2em;
                }
    </style>
}

@section body {


    @{
        StringBuilder sb = new StringBuilder();

        sb.Append("<ul class=\"loading\">");
        for (int i = 1; i <= 30; i++)
        {
            string imageUrl = Html.F().PageManager.Source.GetEmbedLoadingImageUrl(i);

            sb.AppendFormat("<li class=\"f-widget-content\"><img src=\"{0}\"><div class=\"title\">{1}</div></li>", imageUrl, i);
        }
        sb.Append("</ul>");

    }

    @Html.Raw(sb.ToString())


}

