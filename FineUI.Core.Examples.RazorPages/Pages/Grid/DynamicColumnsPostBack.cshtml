@page
@model FineUI.Core.Examples.RazorPages.Pages.Grid.DynamicColumnsPostBackModel
@{
    ViewBag.Title = "Grid/DynamicColumnsPostBack";
    var F = Html.F();
}

@section body {

    <f:DropDownList ID="DropDownList1" Label="切换表格" Width="500"
                    OnSelectedIndexChanged="@Url.Handler("DropDownList1_SelectedIndexChanged")" OnSelectedIndexChangedParameter1="@(new Parameter("selected", "this.getValue()"))">
        <Items>
            <f:ListItem Text="表格一（单选，行扩展列）" Value="table1" Selected="true"></f:ListItem>
            <f:ListItem Text="表格二（多选，多表头，全选列）" Value="table2"></f:ListItem>
        </Items>
    </f:DropDownList>
    <br>
    <br>
    <f:Grid ID="Grid1" IsFluid="true" ShowBorder="true" ShowHeader="true" Title="表格一（单选，行扩展列）"
            DataIDField="Id" DataTextField="Name" EnableMultiSelect="false" EnableCheckBoxSelect="false"
            DataSource="@DataSourceUtil.GetDataTable()" Columns="@ViewBag.Grid1Columns">
    </f:Grid>
    <br>
    <br>

    <f:Button Text="选中了哪些行" ClickHandler="onNotifySelectedRowsClick"></f:Button>

    <br>
    <br>
    注：此功能有很大的局限性，不支持单元格编辑，不支持在回发时改变是否排序、是否分页和是否过滤等属性。
}


@section script {

    <script src="~/res/js/grid.js"></script>

}
