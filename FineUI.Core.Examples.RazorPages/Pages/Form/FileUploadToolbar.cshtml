@page
@model FineUI.Core.Examples.RazorPages.Pages.Form.FileUploadToolbarModel
@{
    ViewBag.Title = "Form/FileUploadToolbar";
    var F = Html.F();
}


@section head {

    <style>
        .photo {
            height: 150px;
            line-height: 150px;
            overflow: hidden;
        }

            .photo img {
                height: 150px;
                vertical-align: middle;
            }
    </style>

}

@section body {

    <f:SimpleForm ID="SimpleForm1" IsFluid="true" BodyPadding="10" EnableCollapse="false" ShowBorder="true" Title="表单">
        <Items>
            <f:Image ID="imgPhoto" CssClass="photo" ImageUrl="~/res/images/blank.png" ShowEmptyLabel="true">
            </f:Image>
            <f:TextBox Label="用户名" ID="tbxUserName" Required="true" ShowRedStar="true">
            </f:TextBox>
            <f:TextBox Label="邮箱" ID="tbxEmail" Required="true" RegexPattern="EMAIL" RegexMessage="请输入有效的邮箱地址" ShowRedStar="true">
            </f:TextBox>
        </Items>
        <Toolbars>
            <f:Toolbar Position="Bottom">
                <Items>
                    <f:Button Text="测试按钮"></f:Button>
                    <f:ToolbarFill></f:ToolbarFill>
                    <f:FileUpload ID="filePhoto" ButtonText="上传个人头像" ButtonIconFont="_Upload" AcceptFileTypes="image/*" ButtonOnly="true" OnFileSelected="@Url.Handler("filePhoto_FileSelected")" OnFileSelectedFields="filePhoto">
                    </f:FileUpload>
                    <f:Button IconFont="_Save" ID="btnSubmit" _ValidateForms="SimpleForm1" Text="提交表单" OnClick="@Url.Handler("btnSubmit_Click")" OnClickFields="SimpleForm1" OnClickParameter1="@(new Parameter("imgPhotoUrl", "F.ui.imgPhoto.el.find('img').attr('src')"))"></f:Button>
                </Items>
            </f:Toolbar>
        </Toolbars>
    </f:SimpleForm>
    <br />
    <f:Label ID="labResult" EncodeText="false">
    </f:Label>

    注：
    <ul>
        <li>
            上传个人头像通过 AcceptFileTypes="image/*" 来控制默认显示的文件类型
        </li>
        <li>
            <a href="http://stackoverflow.com/questions/181214/file-input-accept-attribute-is-it-useful" target="_blank">AcceptFileTypes 的有效值</a>
        </li>
    </ul>

}

