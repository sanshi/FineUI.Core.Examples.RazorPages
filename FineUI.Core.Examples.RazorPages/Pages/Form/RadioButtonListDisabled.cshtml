@page
@model FineUI.Core.Examples.RazorPages.Pages.Form.RadioButtonListDisabledModel
@{
    ViewBag.Title = "Form/RadioButtonListDisabled";
    var F = Html.F();
}


@section body {

    <f:SimpleForm ID="SimpleForm1" IsFluid="true" LabelWidth="160" EnableCollapse="false" BodyPadding="10" ShowBorder="true" ShowHeader="true" Title="简单表单">
        <Items>
            <f:RadioButtonList ID="RadioButtonList1" Label="列表一">
                <f:RadioItem Text="可选项 1" Value="value1" Enabled="false" />
                <f:RadioItem Text="可选项 2" Value="value2" />
                <f:RadioItem Text="可选项 3" Value="value3" />
            </f:RadioButtonList>
            <f:Button ID="btnServerSetSelectedValue" CssClass="marginr" Text="选中上面列表中的第一项" OnClick="@Url.Handler("btnServerSetSelectedValue_Click")"></f:Button>
            <f:Button ID="btnServerGetSelectedValue" CssClass="marginr" Text="获取上面列表的选中项" OnClick="@Url.Handler("btnServerGetSelectedValue_Click")" OnClickParameter1="@(new Parameter("selected", "getRadioButtonList1Selected()"))"></f:Button>
            <f:Button ID="btnClientGetSelectedValue" Text="获取上面列表的选中项（JavaScript 脚本）" ClickHandler="onAlertSelectedValueClick"></f:Button>
            <f:Label>
            </f:Label>
            <f:RadioButtonList ID="RadioButtonList2" Label="列表二（一列）" ColumnNumber="1" DataTextField="Name" DataValueField="Id" DataEnabledField="Enabled" DataSource="@ViewBag.RadioButtonList2DataSource" SelectedValue="@ViewBag.RadioButtonList2SelectedValue">
            </f:RadioButtonList>
        </Items>
        <Toolbars>
            <f:Toolbar ID="Toolbar1" Position="Bottom" ToolbarAlign="Right">
                <Items>
                    <f:Button ID="btnSubmit" _ValidateForms="SimpleForm1" Text="提交表单" OnClick="@Url.Handler("btnSubmit_Click")" OnClickFields="SimpleForm1"></f:Button>
                </Items>
            </f:Toolbar>
        </Toolbars>
    </f:SimpleForm>

}


@section script {
    <script type="text/javascript">

        function onAlertSelectedValueClick(event) {
            var list1 = F.ui.RadioButtonList1;

            var checkedValue = list1.getValue();
            if (checkedValue) {
                top.F.alert("列表一的选中项：" + checkedValue);
            } else {
                top.F.alert("列表一没有选中项！");
            }
        }


        function getRadioButtonList1Selected() {
            var list = F.ui.RadioButtonList1;

            var result = {};
            $.each(list.items, function (index, item) {
                if (item.isChecked()) {
                    result = {
                        'label': item.getInputLabel(),
                        'value': item.getInputValue()
                    };

                    return false; // break
                }
            });

            return F.toJSON(result);
        }

    </script>
}
