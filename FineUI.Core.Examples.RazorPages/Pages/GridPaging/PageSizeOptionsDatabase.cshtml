@page
@model FineUI.Core.Examples.RazorPages.Pages.GridPaging.PageSizeOptionsDatabaseModel
@{
    ViewBag.Title = "GridPaging/PageSizeOptionsDatabase";
    var F = Html.F();
}


@section body {

    <f:Grid ID="Grid1" IsFluid="true" ShowBorder="true" ShowHeader="true" Title="表格（数据库分页 + 页大小选择器）" DataIDField="Id" DataTextField="Name" EnableCheckBoxSelect="true" AllowPaging="true" PageSize="5" IsDatabasePaging="true"
            ShowPageSizeSelector="true" _PageSizeOptions="5,10,15,20"
            RecordCount="@ViewBag.Grid1RecordCount" DataSource="@ViewBag.Grid1DataSource" Height="350">
        <Columns>
            <f:RowNumberField />
            <f:RenderField HeaderText="姓名" DataField="Name" />
            <f:RenderField HeaderText="性别" DataField="Gender" FieldType="Int" RendererFunction="renderGender" />
            <f:RenderField HeaderText="入学年份" DataField="EntranceYear" FieldType="Int" />
            <f:RenderCheckField HeaderText="是否在校" DataField="AtSchool" RenderAsStaticField="true" />
            <f:RenderField HeaderText="所学专业" DataField="Major" RendererFunction="renderMajor" ExpandUnusedSpace="true" MinWidth="150" />
            <f:RenderField HeaderText="分组" DataField="Group" RendererFunction="renderGroup" />
            <f:RenderField HeaderText="注册日期" DataField="LogTime" FieldType="Date" Renderer="Date" RendererArgument="yyyy/MM/dd" />
        </Columns>
        <Listeners>
            <f:Listener Event="paging" Handler="onGrid1PageIndexChanged"></f:Listener>
            <f:Listener Event="pagesizechange" Handler="onGrid1PageSizeChange"></f:Listener>
        </Listeners>
    </f:Grid>
    <br />
    <f:Button Text="选中了哪些行" ClickHandler="onNotifySelectedRowsClick"></f:Button>

}


@section script {

    <script src="~/res/js/grid.js"></script>

    <script>

        function doPostBackReBindGrid() {
            var grid1 = F.ui.Grid1;

            // 触发后台事件（grid1.pageSize 已是切换后的新值，grid1.pageIndex 已重置为 0）
            F.doPostBack({
                url: '@Url.Handler("Grid1_ReBindGrid")',
                params: {
                    fields: grid1.fields,
                    pageIndex: grid1.pageIndex,
                    pageSize: grid1.pageSize
                }
            });

        }

        function onGrid1PageIndexChanged(event) {
            doPostBackReBindGrid();
        }

        function onGrid1PageSizeChange(event, newPageSize, oldPageSize) {
            doPostBackReBindGrid();
        }

    </script>
}
