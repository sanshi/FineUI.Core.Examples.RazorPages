@page
@model FineUI.Core.Examples.RazorPages.Pages.GridOther.EmptyIconTextModel
@{
    ViewBag.Title = "GridOther/EmptyIconText";
    var F = Html.F();
}


@section body {

    <f:Grid ID="Grid1" Height="350" IsFluid="true" ShowBorder="true" ShowHeader="true" Title="表格" DataIDField="Id" DataTextField="Name"
            ShowEmptyIconText="true"
            DataSource="@ViewBag.Grid1DataSource">
        <Columns>
            <f:RowNumberField />
            <f:RenderField HeaderText="姓名" DataField="Name" />
            <f:RenderField HeaderText="性别" DataField="Gender" FieldType="Int" RendererFunction="renderGender" />
            <f:RenderField HeaderText="入学年份" DataField="EntranceYear" FieldType="Int" />
            <f:RenderCheckField HeaderText="是否在校" DataField="AtSchool" RenderAsStaticField="true" />
            <f:RenderField HeaderText="所学专业" DataField="Major" RendererFunction="renderMajor" ExpandUnusedSpace="true" MinWidth="150" />
            <f:RenderField HeaderText="分组" DataField="Group" RendererFunction="renderGroup" />
            <f:RenderField HeaderText="注册日期" DataField="LogTime" FieldType="Date" Renderer="Date" RendererArgument="yyyy/MM/dd" />
        </Columns>
        <Attributes>
            <f:Attribute Key="data-source-key" Value="@ViewBag.Grid1DataSourceKey"></f:Attribute>
        </Attributes>
    </f:Grid>
    <br />
    <f:Button ID="Button1" Text="重新绑定表格" CssClass="marginr"
              OnClick="@Url.Handler("Button1_Click")"
              OnClickParameter1="@(new Parameter("fields", "F.ui.Grid1.fields"))"
              OnClickParameter2="@(new Parameter("source", "F.ui.Grid1.getAttr('data-source-key')"))"></f:Button>

    <br>
    <br>
    注：默认（不设置 EmptyText、ShowEmptyIconText 为 false）表格为空时显示空白；本例通过 ShowEmptyIconText="true" 开启内置的「图标 + 文案」空状态。也可在 PageManager 上设置 GridShowEmptyIconText="true" 全局开启。

}


@section script {

    <script src="~/res/js/grid.js"></script>

}
