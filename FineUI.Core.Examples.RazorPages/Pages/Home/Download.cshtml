@page
@model FineUI.Core.Examples.RazorPages.Pages.Home.DownloadModel
@*
    公共下载入口：读取保存在 wwwroot 之外（~/App_Data/upload）的上传文件。
    图片上传示例与 WebUploader 示例共享此页。输出形态由调用方声明：
    BaseUploadModel.GetImageUrl(fileName) 带 inline=1（图片内联显示），
    BaseUploadModel.GetFileUrl(fileName) 不带（一律作为附件下载）。
*@
