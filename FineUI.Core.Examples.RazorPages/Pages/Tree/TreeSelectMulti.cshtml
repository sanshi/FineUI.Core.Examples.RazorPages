@page
@model FineUI.Core.Examples.RazorPages.Pages.Tree.TreeSelectMultiModel
@{
    ViewBag.Title = "Tree/TreeSelectMulti";
    var F = Html.F();
}


@section body {

    <f:Tree ID="Tree1" IsFluid="true" ShowHeader="true" Title="树控件" EnableMultiSelect="true" _SelectedNodeIDArray="zhumadian,luohe">
        <Nodes>
            <f:TreeNode Text="中国" Expanded="true" NodeID="china">
                <f:TreeNode Text="河南省" Expanded="true" NodeID="henan">
                    <f:TreeNode Text="驻马店市" Expanded="true" NodeID="zhumadian">
                        <f:TreeNode Text="遂平县" NodeID="suiping">
                        </f:TreeNode>
                        <f:TreeNode Text="西平县" NodeID="xiping">
                        </f:TreeNode>
                    </f:TreeNode>
                    <f:TreeNode Text="漯河市" NodeID="luohe" />
                </f:TreeNode>
                <f:TreeNode Text="安徽省" Expanded="true" NodeID="anhui">
                    <f:TreeNode Text="合肥市" NodeID="hefei">
                    </f:TreeNode>
                    <f:TreeNode Text="黄山市" NodeID="huangshan">
                    </f:TreeNode>
                </f:TreeNode>
            </f:TreeNode>
        </Nodes>
    </f:Tree>
    <br>
    <br>
        注：这是一个可以多选的树，你可以按着 Ctrl 或 Shift 选择多项。
    <br>
    <br>

    <f:Button ID="btnGetSelectedValues" OnClick="@Url.Handler("btnGetSelectedValues_Click")" OnClickParameter1="@(new Parameter("selectedNodes", "getSelectedNodes()"))" CssClass="marginr" Text="获取选中的节点列表"></f:Button>

    <f:Button ID="btnSelectOthers" OnClick="@Url.Handler("btnSelectOthers_Click")" Text="继续选中[合肥市]和[黄山市]"></f:Button>
    <br>

    <f:Label ID="labResult" EncodeText="false"></f:Label>
    <br>
    <br>


}



@section script {

    <script>
        function getSelectedNodes() {
            var result = [];

            var selectedNodes = F.ui.Tree1.getSelectedNodes(true);
            if (selectedNodes.length) {
                $.each(selectedNodes, function (index, node) {
                    result.push({
                        id: node.id,
                        text: node.text
                    });
                });
            }

            return F.toJSON(result);
        }
    </script>


}